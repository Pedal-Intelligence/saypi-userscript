# Constitution-vs-reality audit — 2026-07-07

First run under the procedure in `doc/constitution-audit.md` (#535). Audited
`origin/main` at `7ebfe60` ("observability: post-release monitoring procedure +
first baseline health report (#526) (#548)") — i.e. *including* the 2026-07-07
governance batch (#524/#525/#526/#527/#529/#533/#534). Executed locally in a fresh
worktree; hermetic (no real-host runs).

**Verdict: 1 genuine drift item (issue #551 filed). Everything else the
constitution claims checks out against reality; 5 open PRs + 1 proposal are
correctly in-flight behind `awaiting-founder` and are not drift.**

## Lens (a) — governance docs vs reality: PASS (with 1 in-flight note)

| Claim | Reality | Result |
| --- | --- | --- |
| `awaiting-founder` label exists (AGENTS.md marker section) | Exists, description "Blocked on a founder decision or sign-off — needs Ross's attention", #B60205 | ✅ |
| Applying it fires `.github/workflows/awaiting-founder-notify.yaml` which @-mentions the founder | Workflow exists; posts a comment @-mentioning `@rosscado` with the standing-queue link | ✅ |
| Merge gate = `npm test` CI + required `e2e` check | Branch protection required contexts are exactly `["test (22.x)","e2e"]`, matching `test.yaml` job `test` (Node 22.x matrix) and `e2e.yaml` job `e2e` | ✅ |
| Spend ledger (#533): runs ledgered to gitignored `.l4-ledger.json`, caps 6/12h + 25/7-day, `SAYPI_L4_CAP_SESSION`/`_WEEK`/`_OVERRIDE` | `.gitignore:107-109` covers the ledger + corrupt sidecars; `scripts/l4-ledger-lib.mjs` implements exactly those env vars + a `caps` block; `node scripts/l4-ledger.mjs report` runs clean (0/6 session, 0/25 week); ledger imported by all three L4 entry points (`layer4cdp.mjs`, `e2e-host-sweep.mjs`, `e2e-dictation-sweep.mjs`) | ✅ |
| `release:status` / `release:freshness` exist (#529/#534) | Both in `package.json` → `scripts/store-status.mjs` (`--freshness`); `--json` + `--stall-days` supported as the routines charter assumes | ✅ |
| Credential rotation runbook (#534) | Lives as the "Rotation & lifetimes" section of `doc/release/publishing-credentials.md`, linked from `doc/release/README.md` | ✅ |
| Every doc `AGENTS.md`/`CLAUDE.md` reference exists | 17 checked (bootstrap, caution map, release README + incident-response + publishing-credentials, synthetic-voice-turn, layer35, layer4-cdp, both sweeps, autonomous-dev-loop, preact conventions, post-release-monitoring, e2e/README, state-machines README, vad README, scheduled-routines) — all present | ✅ |
| Every npm alias the governing docs cite exists | `release:preflight/plan/packet/bump/build/verify/tag/finalize/submit/status/freshness`, `e2e:build`, `test:e2e`, `layer4cdp:seed/diagnose/verify/self-test`, `layer35:verify`, `e2e-host-sweep`, `e2e-dictation-sweep` — all present | ✅ |
| Memory-vs-docs precedence line | Present (`AGENTS.md` Precedence bullet, Assistant/Agent Docs section) | ✅ |
| `.cursor/rules` staleness warning still accurate | `build_process.mdc` still describes the Webpack-era build (`webpack.config.js` globs, `scripts/update-manifest.js`, `public/saypi.user.js`) — the warning remains correct and necessary | ✅ |
| `doc/scheduled-routines.md` claims | Notify workflow exists ✅; `release:status -- --json` works ✅; freshness check now exists (charter's "when it exists" hedging updated in this PR); `doc/release/delegation-criteria.md` correctly framed as "once PR #538 lands" — in-flight, not asserted as present | ✅ |
| High-blast-radius path enumeration vs the mechanical guard | `scripts/path-guard-lib.mjs` is **not on main** — it ships with PR #539 (open, `awaiting-founder`), so absence is **in-flight, not drift**. Compared against the PR branch: the lib gates `src/JwtManager.ts`, `src/auth/`, `wxt.config.ts`, **plus** `entrypoints/saypi.content.ts` + `entrypoints/saypi-universal.content.ts` (where the injection-scope `matches` actually live), release machinery, `.github/workflows/`, and itself. AGENTS.md's clause says "content-script injection scope (`wxt.config.ts`)" — narrower wording than the lib's deliberate, comment-documented refinement. **Note for whoever merges #539:** align the AGENTS.md parenthetical with the shipped pattern list in the same change. | ☑ in-flight |

## Lens (b) — Layer-3 harness health: PASS + 1 drift

- `npm run e2e:build` — clean (dev-mode static build, manifest guard satisfied).
- `npm run test:e2e` — **13 passed, 0 failed, 46.1s** (all 12 runtime specs,
  including both lifecycle specs and the opus-upload WebCodecs path).
- Full `npm test` in the same worktree — green (tsc + Jest + Vitest; Vitest alone:
  216 files, 1992 passed / 1 skipped).
- **Spec inventory vs `e2e/README.md` — DRIFT.** The README's "Specs" diagram
  documents 7 of the 12 runtime specs (6 in the diagram + settings.e2e.ts in prose). Undocumented: `opus-upload`,
  `pi-action-buttons`, `pi-call-button`, `synthetic-audio-stt` (the spec other docs
  tell readers to copy), `telemetry-gate`. → **Filed #551** (no existing open or
  closed issue covers it; #291 is coverage roadmap, not doc inventory).
- README posture claims spot-checked: `settings.visual.ts` documented as on-demand /
  not in the gate ✅; fail-closed `MAP * ~NOTFOUND` host-resolver claim matches the
  documented launch args ✅.

## Lens (c) — Layer-4 harnesses + ops tooling (static): PASS

- All scripts the constitution points at exist: `layer4cdp.mjs`(+lib),
  `layer35.mjs`, `dev-rig.mjs`, `e2e-host-sweep.mjs`(+lib),
  `e2e-dictation-sweep.mjs`(+lib), `l4-ledger.mjs`(+lib), `store-status.mjs`,
  `release.mjs`.
- `layer4cdp` doc vs script: subcommands `seed | diagnose | verify | self-test` all
  implemented and all have npm aliases; doc's flag examples (`--no-turn`,
  `--headless`) match the script surface. ✅
- Dictation-sweep doc vs `TARGETS` in `e2e-dictation-sweep-lib.mjs`: `fixture` /
  `mistral` / `grok` (incl. the #509 Grok addition + its sign-in caveat) —
  consistent both ways. ✅
- Host-sweep script drives pi.ai / claude.ai / chatgpt.com per its doc. ✅
- Ledger/caps wiring: see lens (a) — verified once, holds. ✅
- `doc/post-release-monitoring.md`'s commands resolve (`release:status` etc.). ✅

## Drift found → issues filed

1. **#551** — `e2e/README.md` spec inventory documents 7 of 12 Layer-3 specs.

## Fixed inline in the audit PR

- `doc/scheduled-routines.md` step 6: the #534 freshness check no longer "when it
  exists" — it exists (`npm run release:freshness -- --json`); wording updated.

## In-flight (correctly awaiting founder — listed, NOT drift)

`awaiting-founder` queue at audit time:

- PR **#547** — secret scanning in CI (gitleaks) (#531)
- PR **#546** — kill-switch-by-default policy + packet checklist (#532)
- PR **#545** — advisory Firefox MV2 smoke lane (first slice of #527)
- PR **#539** — path-guard mechanical check (#530) — see the enumeration-alignment
  note in lens (a)
- PR **#538** — staged release-delegation criteria PROPOSAL (#523); until it lands,
  `doc/release/delegation-criteria.md` intentionally does not exist and
  `doc/scheduled-routines.md` correctly conditions on it

Labeled *issues* in the same queue at audit time (the marker applies to both): #523
(release-delegation decision), #525 (routine activation — GitHub connect), #527
(Firefox lane sign-off), #528 (cross-repo PR mandate), #530 (path-guard sign-off +
promotion), #531 (secret-scan sign-off + promotion), #532 (kill-switch policy merge).

Related but not blocked: issue #544 (kill-switch gap for the voices redesign) is an
open engineering issue, tracked normally.

## Next audit

First Monday of August 2026 (2026-08-03), 06:30 UTC via the cloud routine if
activated by then (`doc/scheduled-routines.md`), otherwise manually from
`doc/constitution-audit.md`.
